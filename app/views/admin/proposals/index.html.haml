
.row
  .small-12.columns
    %h4 All proposals

    %table
      %thead
        %th db id#
        %th Title
        %th Initial proposal by
        %th
          Pledged 
          = ENV['currency_symbol']
        %th &nbsp;    
      %tbody
        - @proposals.each do |proposal|
          %tr
            %td
              %small= proposal.id.to_s
            %td= proposal.name
            %td
              = proposal.user.name
            %td= proposal.pledges.to_s
          